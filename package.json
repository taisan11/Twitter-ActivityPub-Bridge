{
  "name": "Twitter-ActivityPub-Bridge",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.7.9",
    "twitter-openapi-typescript": "^0.0.54"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}